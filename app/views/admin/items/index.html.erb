<% @items.each do |item| %>
  <%= image_tag (item['smallImageUrls'][0]) %>
  <%= link_to item.name, "#{item.url}" %>
  <br>
  <%= link_to admin_item_path(item) do%>
    <%= item.code %>
  <% end %>
  <br>
<% end %>